steps to follow after clone:

1. npm install
2. create '.env' and have a mongodb account for databaseConnection.
      - .env structure is:
            - DB_USER="<your Mongodb Username>"
            - DB_PASSWORD="<your Mongodb Password>"
            - PORT=<optional> // if left blank will run on 5001

